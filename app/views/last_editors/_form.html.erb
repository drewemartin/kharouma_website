<%= form_for(@last_editor) do |f| %>
  <% if @last_editor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@last_editor.errors.count, "error") %> prohibited this last_editor from being saved:</h2>

      <ul>
      <% @last_editor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :article_id %><br>
    <%= f.number_field :article_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
